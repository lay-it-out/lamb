Example 1:

```haskell
id = id + id
    where id = id + id
```

Example 2:

```haskell
id
    = id + id
id = id + id
```

Example 3:

```haskell
id = id + id
    where id = id + id
          id = id + id
```

Example 4:

```haskell
id = id + id where id = id + id
```
