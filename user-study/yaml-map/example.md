Example 1:

```yaml
id :
    id : id
    id :
         id
    id :
```

Example 2:

```yaml
id :
    ? id
    : id
    id : id
```

Example 3:

```yaml
id :
    ? id :
            id
    : id :
            id
```